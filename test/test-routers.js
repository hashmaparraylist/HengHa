let routers = require('../lib/admin/routers/');
let assert = require('chai').assert;

describe('router moudle', () => {
  describe('#api interface', () => {
    it('dummy', () => {
      assert.isOk(true);
    });
  });
});
