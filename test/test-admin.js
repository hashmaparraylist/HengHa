let adminEndpoint = require('../lib/admin/');
let assert = require('chai').assert;

describe('admin module', () => {
  describe('#init()', () => {
    it('dummy', () => {
      assert.isOk(true);
    });
  });
});
