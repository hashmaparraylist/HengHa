let gateway = require('../lib/gateway/');
let assert = require('chai').assert;

describe('gateway module', () => {
  describe('#init()', () => {
    it('dummy', () => {
      assert.isOk(true);
    });
  });
});
